import 'mocha'
import { expect } from 'chai'

describe('testing the test', () => {
    it('should be a good test', () => {
        console.log('testing test complete')
        expect(true).to.eq(true)
    })
})
